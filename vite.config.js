import { defineConfig } from 'vite';

export default defineConfig({
	base: '/05-blackjack-vite-refactor/', //Establece una ruta base a partir de la cual buscará los archivos (importante para deplegar en GitHub Pages)
	plugins: [] //Se pueden añadir plugins adicionales (consultar documentación)
});
