import { defineConfig } from 'vite';

export default defineConfig({
	base: '/js-vite-blackjack/', //Establece una ruta base a partir de la cual buscará los archivos (importante para deplegar en GitHub Pages)
	plugins: [] //Se pueden añadir plugins adicionales (consultar documentación)
});
